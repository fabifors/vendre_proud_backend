# Slack backend for vendre proud project

This is a small auth server for our school project Proud built for vendre

## How to run:

* Install all packages with `npm install`
* Run `node index.js`

Now the server should be up and running.
